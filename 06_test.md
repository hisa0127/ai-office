# テスト報告: AI Office バックグラウンド起動の完了判定(第2回・再確認)

- 作成: ai-tester / 最終更新: 2026-08-17
- 対象: `~/Desktop/ai-office/src/ai_office/scanner.py`(未コミットの修正・2回目)
- 判定サマリ: **PASS 23 / FAIL 3 / 未実施 4 / Blocker残 0**
- 結論: **BUG-001(Blocker)・BUG-002(Major)はいずれも実データで解消を確認。Blockerは残っていない。**
  検収(07)に進める。ただし **BUG-003(docstring の誤り)と、新たに起票した BUG-004(テストの穴)は
  公開前に直すことを推奨**する(いずれも実害は現時点で0件だが、直すコストが極めて小さい)。

> テスターは `src/` `tests/` を一切変更していない。検証はすべて
> (a) 実ログ行の再生、(b) コピーに対する変異テスト、(c) import による読み取り実行 で行った。
> 変異テストは `/private/tmp/.../scratchpad/` にコピーした複製に対してのみ実施している。

---

## 今回の検証で使った実データ

- 対象: `~/.claude/projects/` 配下の **全37ファイル / 236MB**(前回は35ファイル・182MB)
- 全文読み込みはせず、行単位ストリーム + 部分文字列フィルタで処理
- 実ログに存在する Agent 起動は **22件**。うち完了通知があるのは5件

### 起動フラグ × tool_result × 通知 の実測分布(22件)

| 件数 | `run_in_background` | `toolUseResult` | `is_error` | 通知 | 正体 |
|---|---|---|---|---|---|
| 14 | `false`(キー有) | dict / `status=completed` | false | 0 | 同期起動 |
| 5 | `true` | dict / `isAsync=true` / `async_launched` | false | 0〜3 | 明示バックグラウンド(うち1件は実行中の本タスク) |
| 2 | **キー無し** | **str** | **true** | 0 | 起動失敗(`Agent type not found`)= BUG-002 |
| 1 | **キー無し** | dict / `isAsync=true` / `async_launched` | false | 2 | 省略起動 = BUG-001 |

**重要**: 1レコードに `tool_result` ブロックが2つ以上入っている例は **0件**。
したがって `_is_async_result(record, block)` がレコード単位の `toolUseResult` を
ブロックの判定に使っていても、取り違えは起きない(TC-R22)。

---

## A. 書き換えられた既存テストの独立確認

### 旧テストが守ろうとしていた性質

```python
def test_internal_metadata_is_not_shown(self):
    """バックグラウンド起動の内部メタデータを成果として表示しない"""
    ...
    self.assertEqual(sc.history[0].summary, "", "内部メタデータが成果として残っている")
```

この1本は、実は **2つの性質を同時に握っていた**。

- **P1**: 内部メタデータ(`Async agent launched successfully...`)を成果サマリとして画面に出さない
- **P2**: その tool_result の時点で稼働を閉じる(`history[0]` が存在する、という前提に含まれる)

### 判定

| 性質 | いまも守られているか | テストで守られているか |
|---|---|---|
| P1 内部メタデータを表示しない | **守られている**(`_result_text()` L134 のフィルタは健在) | **守られていない**(変異M1が素通り) |
| P2 その場で閉じる | **意図的に捨てられた**(正しい判断) | 新テストが逆の性質を固定済み |

**P2 を捨てた判断は実データに照らして妥当。** 旧テストの入力(`run_in_background` キー無し +
内部メタデータ本文)は、実ログでは **`isAsync:true` を伴って届く**(上表の1件)。
P2 を守ることは BUG-001 そのものを守ることであり、旧テストの期待値のほうが誤っていた。
実ログ `a268deb8` L50/L51/L59 の原文を流すと、修正後は **97.792秒**(実測どおり)で退勤する(TC-R12)。

**ただし P1 をテストごと捨てたのは不当。** 変異テストで確認した:

```
[★素通り★] M1 _result_text の内部マーカー除去をやめる → 32件すべてPASS
```

`_result_text()` から内部マーカー除去を丸ごと削っても、**32件のテストは1本も落ちない**。
P1 は現在ノーガードで、次に触る人が消しても誰も気づけない。
なお P1 は同期経路では到達不能になった(内部マーカーがあれば `_is_async_result()` が先に非同期と判定するため)が、
**`is_error` 経路(`_finish(..., summary=_result_text(b))`)では今も生きている**。
死んだ性質ではないので、テストは復活させるべき。

### 旧テストが正しく新挙動が誤り、という可能性の検討

検討したが **否定される**。根拠は、旧テストの入力形状(`run_in_background` キー無し +
`toolUseResult` が dict でない + 内部メタデータ本文)が **実ログに1件も存在しない**こと。
実在するキー無し起動は2種類しかなく、どちらも新実装が正しく扱う(片方は `isAsync:true`、
もう片方は `is_error:true`)。旧テストは実在しない形状を固定していた。

---

## B. status のホワイトリスト化(`completed` のみ成功)

### 全ログ走査の結果

`<task-notification>` を含む行を全37ファイルから抽出し、**全JSON文字列値を再帰的に走査**して
「本物の配送(値の先頭が `<task-notification>`)」だけを数えた。

| status | 件数 |
|---|---|
| `completed` | 13 |
| `failed` | 5 |
| `killed` | 3 |
| **上記以外** | **0** |
| **`<status>` タグ自体が無い配送** | **0** |

- **`completed` 以外に成功を意味する status は実ログに存在しない**(TC-R18)。
  したがってホワイトリスト化によって正常終了が失敗として記録されることはない。
- **`status` タグの無い通知形式も存在しない**(TC-R19)。`_status_is_ok(None) -> True` の分岐は
  実データ上 **一度も通らない**。
- `killed` は実ログ原文の3形式すべてで正しく `ok=False` になる(TC-R17)。
  ダッシュボード上も「中断ステータス確認用の実験 / 2.9s / NG」として現れる。

### 指摘(Minor・BUG-006 として起票)

`_status_is_ok(None) -> True` は、同じ関数のコメントが掲げる方針
「知らない値は成功にしない = 間違っていても画面に出るので気づける」と**逆向き**。
実データが0件なので実害はないが、方針が関数内で割れている。
変異テスト M9(`None` を失敗扱いに変更)も **素通り**した = どちらの挙動も固定されていない。

---

## C. 退行の確認(実データ)

**通知4形式すべてを実ログの原文行のまま単独で流し、全形式で正しく退勤することを確認した。**

| 形式 | 実ログの出典 | 結果 |
|---|---|---|
| `queue-operation` / `enqueue` | `8091fe95` L3272 | **PASS**(1537.5s / ok=True) |
| `queue-operation` / `remove` | `8091fe95` L3273 | **PASS** |
| `user`(`message.content` が str) | `8091fe95` L3163 | **PASS**(status=failed → ok=False) |
| `attachment`(`attachment.prompt`) | `8091fe95` L3276 | **PASS** |
| 3形式まとめて配送 | L3272+L3273+L3276 | **PASS**(稼働は1回のみ計上) |

前回PASSした誤検知防止も全て維持:

- 引用(assistant の発言)で誤完了しない → PASS
- **Bash stdout に本物の `tool-use-id` と `<status>failed</status>` が埋まった L3340** で誤完了しない → PASS
- 別 tool_use_id 宛の通知を無視 → PASS

### 実ログ全体のエンドツーエンド

全37ファイル(236MB)を現行 scanner で走査。**所要 0.58秒 / 履歴21件 / 稼働中1件**。

- 稼働中1件 = **本タスク(ai-tester)自身**。永久稼働中の残骸ではない(通知が未着なだけ)
- 前回 BUG-001 の実害だった `（案件名は匿名化）提案書を再作成` は **0.0s → 97.8s** に是正
- BUG-002 の2件(`Agent type not found`)は **稼働中に残らず、0.0s / NG として記録**される

### 32件のユニットテスト

`python3 -m unittest discover -s tests` → **Ran 32 tests / OK**(2回実行、いずれも全通過)。

---

## D. 新しい壊れ方の確認

### D-1. `is_error` 早期クローズと非同期判定の順序 → 問題なし

実装の順序は `is_error` → 非同期判定。実データ上、両者が同時に立つレコードは存在しない
(`is_error:true` の2件はいずれも `toolUseResult` が **str** で `isAsync` を持たない)。
順序を入れ替えた場合の取りこぼしも、実ログ原文で確認した限り発生しない。

追加で以下の順序も実ログ行で試し、いずれも正しく1回だけ計上された:

- 起動 → 起動通知 → **完了通知** → あとから `is_error` … 通知優先で閉じ、後続は空振り(PASS)
- 起動 → **完了通知** → あとから起動通知(順序逆転) … 正しく閉じる(PASS)

### D-2. マーカー本文による非同期判定のフォールバックは、実データで一度も発火していない

`_is_async_result()` の第2段(`_INTERNAL_MARKERS` の部分一致)を計測した:

```
_is_async_result 呼び出し 15回 / dict分岐でTrue 1回 / マーカー分岐でTrue 0回 / False 14回
```

**マーカー分岐は実ログ全体で0回**。一方でこの分岐は
`"launched successfully"` という**極めて緩い部分一致**を含む。
同期エージェントの報告文にこの語が入ると、その稼働は **完了通知が来ないので永久に稼働中**になる。
修正前は「サマリが空になる」だけで済んでいたので、**失敗の質は悪化する方向**。

現時点の実害は0件(同期扱いの結果16件中、マーカー誤爆 **0件**)。
ただし利益0・リスク非0の分岐なので、削除を推奨(BUG-005)。

### D-3. 誤検知防止の3つのガードが、単体ではどれもテストに固定されていない

変異テスト10種のうち **5種が素通り**した。

| 変異 | 内容 | 32件テスト |
|---|---|---|
| M1 | `_result_text` の内部マーカー除去を削除 | **素通り** |
| M2 | `killed` を成功扱いに戻す | 検出 |
| M3 | status 判定を `!= failed` に戻す | 検出 |
| M4 | `is_error` 早期クローズを削除 | 検出 |
| M5 | 非同期判定を起動時フラグだけに戻す(BUG-001再発) | 検出 |
| M6 | `run.background or` を外す | 素通り(実データ上は等価。可) |
| M7 | `_as_text` で `type!=text` のブロックも連結 | **素通り** |
| M8 | `assistant` 除外をやめる | **素通り** |
| M10 | 通知本文の先頭一致を部分一致にゆるめる | **素通り** |

**M7 と M10 を同時に入れると、実ログ原文で誤完了が起きる。**

```
起動 8091fe95 L3157(まだ稼働中) + 引用行 L3340 を流す
  無変異(現行実装)   {'open': 1, 'hist': 0}   正しい
  M7 のみ           {'open': 1, 'hist': 0}   正しい
  M10 のみ          {'open': 1, 'hist': 0}   正しい
  M7+M10           {'open': 0, 'hist': 1, 'ok': [False]}   ★誤完了★
```

L3340 は Bash の stdout で、`tool_result.content` が **str**、
マークの位置は **42文字目**(先頭ではない)、埋まっている ID は本物の
`toolu_01GHnk6Azm6Fn8zukDWNwoKL`、status は `failed`。
つまり「稼働中のエージェントが、ログを grep しただけで失敗退勤する」。
そして **M7+M10 を入れても32件のテストは全部PASSする**。

現行実装は正しい。守っているのは `type=="text"` フィルタと `startswith` の**2枚重ね**だが、
**どちらもテストで固定されていない**。前回報告の「追加すべきテスト」4番がそのまま未実施。

### D-4. 修正の副作用: 15分を超える正常稼働が「滞留」と表示される(新規)

BUG-001 修正でバックグラウンド稼働が実時間で開き続けるようになった結果、
`config.py` の既定 `stallMinutes: 15` を超える稼働が **滞留(stalled)** 扱いになる。

実データ21件中 **3件が15分超**:

| 実働 | エージェント | 結末 |
|---|---|---|
| 91.8分 | ai-director | ok=False |
| 28.3分 | ai-tester | ok=True |
| 25.6分 | ai-director | ok=True |

`StateBuilder.build()` を実行した時点でも、実行中の ai-tester(本タスク)は
`"status": "stalled"` として出力されている。**正常に働いているエージェントが警告表示になる。**
修正前はバックグラウンド稼働が0.02秒で閉じていたため一度も起きなかった現象で、
**この修正が初めて表に出した挙動**。BUG-007 として起票。

---

## テストケース

実行スクリプトは `/private/tmp/claude-501/.../scratchpad/`(成果物外)。
入力は特記なき限り **実ログの原文行をそのまま使用**している。

| TC-ID | 観点 | 手順(要約) | 期待結果 | 結果 | 実施日 |
|---|---|---|---|---|---|
| TC-R01 | 回帰 | `unittest discover -s tests` | 32件全通過 | **PASS** | 2026-08-17 |
| TC-R02 | C | `queue-operation/enqueue`(L3272)単独 | 退勤・ok=True・1537.5s | **PASS** | 2026-08-17 |
| TC-R03 | C | `queue-operation/remove`(L3273)単独 | 退勤 | **PASS** | 2026-08-17 |
| TC-R04 | C | `user`(str)形式(L3163)単独 | 退勤・ok=False | **PASS** | 2026-08-17 |
| TC-R05 | C | `attachment`(L3276)単独 | 退勤 | **PASS** | 2026-08-17 |
| TC-R06 | C | 3形式まとめて配送 | 稼働1回のみ計上 | **PASS** | 2026-08-17 |
| TC-R07 | C | bg起動の tool_result(L3179) | 退勤しない | **PASS** | 2026-08-17 |
| TC-R08 | C | assistant の引用(L3230) | 誤完了しない | **PASS** | 2026-08-17 |
| TC-R09 | C | Bash stdout の引用(L3340・本物ID埋込) | 誤完了しない | **PASS** | 2026-08-17 |
| TC-R10 | C | 別ID宛の通知(L3161) | 無視される | **PASS** | 2026-08-17 |
| TC-R11 | BUG-001 | 省略起動(a268deb8 L50/L51) | 退勤しない | **PASS** | 2026-08-17 |
| TC-R12 | BUG-001 | 省略起動 + 通知(L59) | **97.792s** で退勤 | **PASS** | 2026-08-17 |
| TC-R13 | BUG-002 | bg起動 + `is_error`(半合成) | その場で失敗として閉じる | **PASS** | 2026-08-17 |
| TC-R14 | BUG-002 | 実ログ原文 5a39d87c L111+L112 | 失敗として閉じる | **PASS** | 2026-08-17 |
| TC-R15 | D-1 | 通知 → あとから `is_error` | 通知優先・1回のみ | **PASS** | 2026-08-17 |
| TC-R16 | D-1 | 通知 → あとから起動通知(順序逆転) | 正しく閉じる | **PASS** | 2026-08-17 |
| TC-R17 | B | `killed` 実験の3形式(L3478/3479/3483) | 全形式で退勤・ok=False | **PASS** | 2026-08-17 |
| TC-R18 | B | 全37ファイルの `<status>` 値を網羅列挙 | completed 以外の成功が無い | **PASS** | 2026-08-17 |
| TC-R19 | B | `<status>` タグ無しの配送を探す | 0件 | **PASS** | 2026-08-17 |
| TC-R20 | C | 全ログ236MBのエンドツーエンド走査 | 0.58s / 稼働中は実行中の1件のみ | **PASS** | 2026-08-17 |
| TC-R21 | D-2 | 同期結果16件に内部マーカーが混ざるか | 誤爆0件 | **PASS** | 2026-08-17 |
| TC-R22 | D-1 | 1レコードに複数 tool_result があるか | 0件(取り違え不能) | **PASS** | 2026-08-17 |
| TC-R23 | — | `StateBuilder.build()` のJSON化 | 例外なく12103文字を生成 | **PASS** | 2026-08-17 |
| TC-R24 | A/D-3 | 変異テスト10種 | 全種が検出される | **FAIL**(5種素通り)→ BUG-004 | 2026-08-17 |
| TC-R25 | D-3 | M7+M10 で実ログ L3340 を流す | 誤完了しない | **FAIL**(誤完了する)→ BUG-004 | 2026-08-17 |
| TC-R26 | D-4 | 15分超の正常稼働の表示 | 稼働中として表示 | **FAIL**(滞留表示)→ BUG-007 | 2026-08-17 |
| TC-R27 | B | `<status>` タグ無し通知の ok 判定 | 仕様どおり | **未実施**(実データ0件) | — |
| TC-R28 | — | SendMessage 再開時の2回目通知 | 二重計上しない | **未実施**(実呼出0件) | — |
| TC-R29 | D-1 | `is_error` と `isAsync` が同時に立つ結果 | 仕様どおり | **未実施**(実データ0件) | — |
| TC-R30 | — | ブラウザでのダッシュボード目視 | 稼働中/退勤が正しく見える | **未実施** | — |

---

## バグ票

### BUG-001 [重要度: Blocker] [状態: **再確認済**]

`run_in_background` を省略した起動が0.02秒で退勤扱いになる

- 再検証: 実ログ `a268deb8` L50(キー無し起動)/ L51(`isAsync:true` `async_launched`)/ L59(完了通知)を原文のまま再生
- 結果: **稼働時間 97.792秒**(実測値と完全一致)。tool_result 時点では退勤しない
- 全ログのエンドツーエンドでも `（案件名は匿名化）提案書を再作成` が **0.0s → 97.8s** に是正
- 判定根拠を `toolUseResult.isAsync` / `status == "async_launched"` に寄せた修正が有効に働いている
- 回帰防止テスト `test_omitted_flag_is_treated_as_async` / `test_omitted_flag_closes_on_notification` は
  変異M5(旧ロジックに戻す)を**正しく検出**する = ガードとして機能している

### BUG-002 [重要度: Major] [状態: **再確認済**]

バックグラウンド起動が失敗したとき永久に「稼働中」になる

- 再検証: 実ログ `5a39d87c` L111(起動)+ L112(`is_error:true` / `Agent type 'ai-director' not found`)を原文のまま再生
- 結果: **`open_runs` に残らず、ok=False として記録**される
- 全ログのエンドツーエンドでも当該2件は `0.0s / NG / "Agent type 'ai-director' not found..."` として履歴に出る
- 前回懸念した「BUG-001を直すとこの2件が永久稼働中に変わる」は**発生していない**
  (`is_error` を非同期判定より先に見る順序が正しく効いている)
- 回帰防止テスト `test_launch_failure_closes_immediately` は変異M4を**正しく検出**する

### BUG-003 [重要度: Minor] [状態: **Open**(未修正)]

docstring・コメントの記述が実態と一致していない(前回から未対応 + 今回の修正で1か所増えた)

1. `scanner.py` L11-14(モジュール docstring)

   > ただし **run_in_background=true の起動だけは別扱いにする。**

   **今回の修正で明確に誤りになった。** 省略起動も別扱いになったのが修正の本体であり、
   この行は修正前の仕様を説明している。ここだけ読んだ人は BUG-001 を再導入しうる。

2. `scanner.py` L106-109(`_notification_text` の docstring)

   > 同じ通知がログに **3通り**の形で現れる / **失敗時** user / **成功時** queue-operation / **成功時** attachment

   実データは **4通り**(`queue-operation` は `enqueue` と `remove` の2行)。
   また **user型/attachment型は成功・失敗と対応しない**。実測:
   user型で `completed`(`a268deb8` L61)、attachment型で `failed`(`e713eef6` L212)、
   attachment型で `killed`(`8091fe95` L3483)がいずれも出現する。
   分岐しているのは結末ではなく**通知時に本体が処理中だったか**。

- なお L77-80 の「実ログで観測できたのは completed / failed / killed の3種類」は
  **今回の全ログ走査と完全に一致**していた(正しい記述)
- 差し戻し先: ai-coder

### BUG-004 [重要度: Major] [状態: Open]

**誤完了を防いでいる3つのガードが、どれもテストで固定されていない**

- 再現手順:
  1. `scanner.py` の `_as_text()` から `b.get("type") == "text"` の絞り込みを外す
  2. `_notification_text()` の `text.lstrip().startswith(_NOTE_MARK)` を `_NOTE_MARK in text` に変える
  3. `python3 -m unittest discover -s tests` を実行する
- 期待結果: いずれかのテストが落ちる
- 実際の結果: **Ran 32 tests / OK**(全通過)。にもかかわらず、実ログ `8091fe95` L3157(稼働中の起動)+
  L3340(Bash stdout の引用)を流すと **稼働中のエージェントが `ok=False` で誤退勤する**
- 単体の変異でも M1 / M7 / M8 / M10 の4種が素通りする(M6は実データ上等価なので許容)
- 影響: いま実装は正しいが、次に `_as_text` や通知抽出を触る人が壊しても**テストも実ログ走査も気づけない**
  (全ログ走査で気づけないのは、L3340 が対象の稼働が閉じた**後**に現れるという偶然によるもの。
  ログを grep しながらエージェントを動かせば順序は簡単に逆転する)
- 差し戻し先: ai-coder

### BUG-005 [重要度: Minor] [状態: Open]

`_is_async_result()` のマーカー部分一致フォールバックが、利益0でリスクだけを持っている

- 実測: 実ログ全体で `_is_async_result` は15回呼ばれ、**マーカー分岐で True になったのは0回**
  (非同期判定はすべて `toolUseResult` の dict 分岐で成立している)
- リスク: マーカーに `"launched successfully"` という緩い部分一致が含まれる。
  同期エージェントの報告文にこの語が入ると、**完了通知が来ないので永久に稼働中**になる。
  修正前の同じ入力は「サマリが空になる」だけだったので、**失敗の質が悪化する方向**
- 現時点の実害: 0件(同期扱いの結果16件中、マーカー誤爆0件)
- 差し戻し先: ai-coder

### BUG-006 [重要度: Minor] [状態: Open]

`_status_is_ok(None) -> True` が、同じ関数のコメントの方針と逆向き

- 該当: `scanner.py` L84-87。コメントは「知らない値は成功にしない(気づける方へ倒す)」と宣言しているが、
  `status` タグが無い場合だけ **成功**に倒している
- 実データ: `<status>` タグの無い配送は **0件**なので実害なし
- 変異M9(`None` を失敗扱いに変更)も素通りする = どちらの挙動もテストで固定されていない
- 差し戻し先: ai-coder(仕様を決めた上でテストを1本足す)

### BUG-007 [重要度: Minor] [状態: Open]

**BUG-001 修正の副作用で、15分を超える正常稼働が「滞留(stalled)」と表示される**

- 再現手順:
  1. バックグラウンドエージェントを15分以上かかる作業で起動する
  2. ダッシュボードを見る
- 期待結果: 稼働中(working)として表示される
- 実際の結果: `config.py` の既定 `stallMinutes: 15` を超えるため **滞留**扱いになる。
  `StateBuilder.build()` の出力でも、実行中の ai-tester が `"status": "stalled"` になっている
- 実データ: 履歴21件中 **3件(91.8分 / 28.3分 / 25.6分)が15分超**。いずれもバックグラウンド起動
- 原因: 修正前はバックグラウンド稼働が0.02秒で閉じていたため、この閾値に一度も触れなかった。
  修正によって実時間で開き続けるようになり、**既定値がバックグラウンド運用に合わなくなった**
- 判断が必要な点(ひさ): 既定値を上げるのか、バックグラウンド稼働は滞留判定から外すのか、
  それとも「長時間=要確認」として現状の表示を正とするのか。**見送り含め判断はひさが行う**
- 差し戻し先: ai-coder(方針決定後)

---

## 追加すべきテスト(`tests/test_core.py` への提案 / テスターは書き込み不可)

前回の提案のうち **3・4・5 が未実施**。1・2 は実装済みで有効に機能している。

1. **`_result_text` の内部マーカー除去を固定する**(旧 `test_internal_metadata_is_not_shown` の P1 を復活)
   - `is_error: true` かつ本文が `"Async agent launched successfully. (This tool result is internal metadata ...)"` の
     tool_result を流し、`history[0].summary == ""` を確認する
   - こうすると変異M1が検出できる。旧テストを消すのではなく、**入力を is_error 経路に付け替えて残す**のが正解だった

2. **`type=user` で `message.content` が list(tool_result)の引用行を無視すること**(変異M7を殺す)
   - 入力は `8091fe95` **L3340** の実物を使う。`tool_result.content` が str で、
     マークが42文字目にあり、本物の `toolu_01GHnk6Azm6Fn8zukDWNwoKL` と `<status>failed</status>` が埋まっている
   - 起動行は `8091fe95` **L3157**(この時点でまだ稼働中)

3. **通知本文が先頭一致でなければ通知として扱わないこと**(変異M10を殺す)
   - 上と同じ L3340 を使い、`open_runs` が減らないことを確認する

4. **`assistant` の発言に含まれる通知を無視すること**(変異M8を殺す)
   - 現行の `test_quoted_notification_in_own_reply_is_ignored` は
     引用の前に「通知の形はこうです:」という**前置きが付いている**ため、
     実際には `startswith` のほうが効いていて assistant 除外を検証できていない。
     **前置きなしで `<task-notification>` から始まる assistant 発言**を入力にすること

5. **`queue-operation` / `operation=remove` の単独配送**
   - 現行 `task_note(shape="queue")` は `operation="enqueue"` しか作らない。
     コード上は同じ分岐だが、実ログには `remove`(`8091fe95` L3273)が独立行として存在する

6. **`<status>` タグの無い通知の扱い**(BUG-006の仕様確定後・変異M9を殺す)

---

## 未確認事項

1. **`<status>` タグ無しの通知**(TC-R27)
   全37ファイル・236MBを走査して **0件**。存在しないものは検証できないため未実施とする。
   `_status_is_ok(None) -> True` の分岐は実データ上一度も通らない。

2. **SendMessage による再開時の再通知**(TC-R28)
   `SendMessage` の**実呼び出しは全ログで0件**(文字列としては17ファイルに出現するが、
   すべてツール一覧の記載であって呼び出しではない)。
   通知本文の `<note>` は「同じ task-id が複数回通知されうる」と述べているが、
   実ログの8つの tool_use_id はいずれも **status が1種類**しか持たず、再開の形跡がない。前回同様、未検証。

3. **`is_error` と `isAsync` が同時に立つ tool_result**(TC-R29)
   実データ0件。仮に存在すると `is_error` が先に評価されて即クローズし、
   後から届く通知は空振りする(= 実働時間が失われる)。実データが無いため PASS/FAIL を付けない。

4. **ブラウザでのダッシュボード目視**(TC-R30)
   `StateBuilder.build()` が例外なくJSONを生成することは確認した(TC-R23)が、
   実際の画面での「稼働中/退勤/滞留」の見え方は未確認。**BUG-007 の見え方は目視で確かめる価値がある**。

5. **バックグラウンド Bash の通知**
   `<task-notification>` はエージェント以外でも発行される。実データで3件確認
   (`e713eef6` L206 `Serve theme directory...` = failed / L248 `Install pricing-simulator...` = completed /
   `8091fe95` L76 `Start dashboard server...` = completed)。
   task-id が短い形式(`b02r6x1a9`)で、`tool_use_id` が `open_runs` に無いため無視される。実害なしを確認済み。

---

## 検収可否

**Blocker は0件。検収(07)に進める。**

- BUG-001(Blocker)・BUG-002(Major)はいずれも **実ログの原文行による再生で解消を確認**した
- 実ログ全体(37ファイル/236MB)を走査しても、永久稼働中の残骸は **0件**
  (稼働中1件は実行中の本タスク自身)
- 32件のユニットテストは全通過し、うちBUG-001/002の回帰防止テストは
  変異テストで**実際に効いている**ことを確認した

---

## 公開判断(PyPI)

**テスターの所見: 機能面では公開可。ただし BUG-003 の1行だけは公開前に直すことを推奨する。**
公開の実行と最終判断はひさが行う(テスターは見送り・実行の権限を持たない)。

### 公開してよいと判断する根拠

| 根拠 | 実データ |
|---|---|
| 主経路(省略起動)が正しく動く | `a268deb8` の原文再生で 97.792秒(実測一致) |
| 起動失敗が在席のまま残らない | `5a39d87c` L111+L112 の原文再生で ok=False クローズ |
| 通知4形式すべてで退勤する | `8091fe95` の4行を単独配送して全PASS |
| 誤完了しない | 本物のIDが埋まった L3340 を流しても稼働中を維持 |
| 中断が成功に混ざらない | 実験ログの `killed` が3形式すべてで ok=False |
| 全ログで残骸ゼロ | 37ファイル/236MB走査で稼働中は実行中の1件のみ |
| 既存機能の退行なし | 32件全通過 + 同期起動14件が実時間で記録される |

### 公開前に直すべきもの(いずれも実害0件・修正コスト小)

1. **BUG-003(Minor / 強く推奨)**: `scanner.py` L11-14 の
   「run_in_background=true の起動だけは別扱いにする」は、**今回の修正で明確に誤りになった**。
   MITで公開する以上、外部の人が最初に読むのがこの docstring になる。
   修正内容そのものを否定する説明が残ったまま公開するのは避けたい。**1〜2行の書き換えで済む。**
2. **BUG-004(Major / 推奨)**: 誤完了防止のガードがテストで固定されていない。
   公開後に外部からPRが来た場合、**テストが通るのに壊れているPR**を弾けない。
   公開直後でなくてよいが、外部の貢献を受ける前には入れておきたい。

### 公開を止める理由にはならないもの

- BUG-005 / BUG-006: 実害0件。内部品質の話であり、利用者に影響しない
- BUG-007: 表示上の警告が出るだけでデータは正しい。ただし**既定値をどうするかはひさの判断**が要る。
  15分を超えるバックグラウンド運用を前提にするなら、公開前に既定値を見直しておくと
  「入れてすぐ滞留だらけに見える」という第一印象を避けられる
